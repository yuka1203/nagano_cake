<h1>商品一覧</h1>

<% @items.each do |item| %>

<div>
    <%=link_to item_path(item.id) do %>
     <%= image_tag item.get_image(200,200) %>
    <% end %><br>
    <%= item.name %><br>
    <%= item.price %>
</div>
<% end %>