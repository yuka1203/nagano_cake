<h1>商品一覧</h1>

<% @items.each do |item| %>

<div>
    <%=link_to items_path(item.id) do %>
     <%= image_tag item.image %>
    <% end %><br>
    <%= item.name %><br>
    <%= item.price %>
</div>
<% end %>